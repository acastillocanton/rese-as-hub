# dependencies
node_modules
.pnp
.pnp.js

# next
.next
out
next-env.d.ts

# production
build
dist

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo

# design package source (keep tracked? — for now ignore the zip/tar)
_design_package/ReseñaHub.zip
_design_package/design.tar

# editor
.vscode/
.idea/

.vercel

# Claude Code local config (settings.local.json, plans/, etc.)
.claude/

# Scripts ad-hoc con datos reales (seed-people.mjs lleva emails y teléfonos
# inline — nunca commitear). Si añades scripts de uso general, usa otra ruta.
scripts/

# Playwright
playwright-report/
test-results/
e2e/.auth/
.playwright/

# Excel lock files (~$ prefix)
~$*.xlsx

# Mensajes de commit temporales (se usan con git commit -F y se borran)
.commit-msg.tmp

# Agente de cosecha de deep-links (§4.54): perfil de Chrome dedicado + logs.
# El código del agente (agent/*.mjs, *.cmd, README) SÍ se versiona.
agent/.chrome-profile/
agent/*.log
